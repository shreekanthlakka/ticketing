import styled from "styled-components";

const Container = styled.div`
    margin: 1rem;
`;

function Logo() {
    return (
        <Container>
            <h2>TicketingApp</h2>
        </Container>
    );
}

export default Logo;
